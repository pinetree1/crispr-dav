This example uses a genome as reference.

Steps to test the pipeline:
1. Modify the paths of applications according to your environment.
2. sh run_crispr.sh &> r.log &

Intermediate files are in 'align' directory. Results are in 'deliverables'
directory.
