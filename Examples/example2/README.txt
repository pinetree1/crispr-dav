This example uses an amplicon sequence as reference.

Steps to test the pipeline:
1. Modify the paths of applications according to your environment.
2. sh run_crispr.sh &> r.log &

Intermediate files will be in 'align' directory. Results will be in 'deliverables'
directory.
