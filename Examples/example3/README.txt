The example3 is similar to example1, but uses a samplesheet. Steps to run pipeline:
    /bfx/app/crispr-dav/prepare_run.pl samplesheet.txt: this generates a directory amp* for each amplicon.
    cd to an amplicon directory 
    sh run.sh
